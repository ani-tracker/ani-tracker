# Ani Tracker 番剧详情功能验收记录

验收日期：2026-07-20

验收分支：`master-stich-ui`

实现提交：`43a81f9 feat: 完善番剧详情页面与元数据聚合`

关联计划：[anime-detail-redesign-plan.md](./anime-detail-redesign-plan.md)

## 1. 验收结论

工程验收通过，Electron 桌面端核心详情链路手工验收通过。

四档固定视口尚未全部形成截图留档，因此本记录结论为“有条件通过”；不影响当前实现提交，但关闭最终 UI 验收前应补齐第 5 节中的截图项。

## 2. 已验收范围

- 新番发现图鉴卡片进入番剧详情。
- 未追番与已追番共用同一详情页面和数据契约。
- 本地 SQLite 详情读取、旧库迁移和损坏 JSON 降级。
- AniList、Bangumi、Mikan 详情字段聚合与增量刷新。
- IPC、preload、renderer API 和远程只读详情调用。
- 已追番概览、进度、偏好、规则、资源和任务入口。
- 添加追番、移除追番、资源搜索意图传递。
- 桌面二级页面返回、面包屑、刷新和更多操作。
- 空字段隐藏、加载骨架、错误、离线和部分来源失败状态。

## 3. 自动化验收

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `pnpm.cmd run typecheck` | 通过 | Node 与 Web TypeScript 检查均无错误 |
| `pnpm.cmd run test:parsers` | 通过 | 218 项测试全部通过，0 失败 |
| `pnpm.cmd build` | 通过 | main、preload、renderer 和 qBittorrent 资源构建完成 |
| `git diff --check` | 通过 | 本功能提交路径无空白字符错误 |
| SQLite schema | 通过 | schema 版本升级至 13，详情 JSON 可往返与迁移 |
| 视图模型测试 | 通过 | 标题、状态、进度、字段隐藏和外链映射受测试覆盖 |

构建仍会输出项目已知的 ESM warning，不影响产物。

## 4. 桌面端手工验收

验收环境：Windows、Electron 开发模式、实际窗口约 `1228x813`。

| 场景 | 结果 | 观察 |
| --- | --- | --- |
| 应用启动 | 通过 | 首页和新番发现无白屏，侧栏与内容区可用 |
| 新番目录加载 | 通过 | 2026 夏季目录加载 38 部，海报和状态正常显示 |
| 卡片进入详情 | 通过 | 点击图鉴卡片后进入同一应用壳内的二级详情页 |
| 已追番详情 | 通过 | 海报、标题、原名、评分、简介和追番概览正常显示 |
| 详情动作 | 通过 | 编辑规则、查看资源、刷新和更多操作入口可识别 |
| 数据降级 | 通过 | 缺失字段未显示虚构占位值，缓存较旧状态有明确标记 |
| 布局稳定性 | 通过 | 已验收窗口下未发现横向溢出、遮挡、双顶栏或双滚动条 |
| 可访问名称 | 通过 | 返回、刷新、详情分区和追番动作均有可识别名称 |

## 5. 固定视口截图清单

| 视口 | 当前状态 | 最终检查重点 |
| --- | --- | --- |
| `1440x900` | 待截图留档 | 三列首屏、标题换行、动作区和侧栏宽度 |
| `1024x768` | 待截图留档 | 收缩侧栏、详情双列和分区导航 |
| `768x1024` | 待截图留档 | 移动头部切换、单列内容和触控目标 |
| `390x844` | 待截图留档 | 无双顶栏、无横向溢出、简介展开和全宽主动作 |

## 6. 回归建议

- 使用未追番条目完成一次“添加追番 -> 原地切换已追番”操作。
- 从详情分别打开规则、资源和任务，再验证返回上下文。
- 在四档固定视口下各保存未追番和已追番截图。
- 深色、跟随系统和一个自定义主题各检查一次正文与焦点对比度。

## 7. 遗留风险

- 当前未保存四档固定视口截图，移动端视觉结论仍需最终留档。
- 外部元数据刷新依赖来源可用性；部分来源失败时应继续保留本地详情。
- 远程端为只读详情，桌面专属动作必须继续由服务端权限白名单约束。
